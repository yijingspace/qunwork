2026-08-10|50af728|fix(automation): reap stale 'running' runs on startup — a turn wedged on a dead provider could leave a run reading 'running' forever across a restart (run_task's finally never ran). start_connectors now marks runs stuck >5min as 'error: interrupted by restart', so history stays truthful and 'Run now' works.
2026-08-10|983fc41|fix(engine): stall-guard a model call — a wedged gateway that streams nothing held the turn forever (scheduled run parked 16+ min; SDK default timeout does not apply to streaming iteration). _astream now aborts after 90s of zero chunks (or 600s total) with a retriable error. User-Stop still returns silently; long-thinking models are unaffected because reasoning chunks count as output.
2026-08-10|5489cd8|fix(automation): duplicate tool_call_id 400 on scheduled runs — _run_scheduled_task now claims the run's session as running for its whole lifetime (incl. parked approvals). Approving a pending tool call used to durable-resume the suspended session (replaying still-unanswered tool_calls via engine.resume) AND release the original engine.run — the same calls executed twice, producing duplicate tool_call_id in history and HTTP 400. Regression test asserts 2 executions without the claim vs 1 with it.
2026-08-10|6ed6f9b|fix: detail-panel 'continue research' button now uses resume-pack too (was sending the raw digest content — fission/cavity showed only the title+summary); resume-by-title tokenizes Chinese titles (marker-stripped) so cavity/gap findings resolve original sources
2026-08-10|bb9347a|research pack resolves ORIGINAL sources: emergent digests ([涌现]/[蜂胞分裂]) now ship the same-topic source files' full body + source path (no more empty/garbled content for the agent); resume-by-title endpoint for non-persisted findings (cavity/gap); resonance related de-duped and self-excluded
2026-08-10|2b5c6b1|fix: knowledge detail view 401 — handleExpand used the GLOBAL fetch which lacks the launch token; desktop sidecar requires X-QunWork-Token so the detail request failed with missing-ok → '无法加载详情'. Export authedFetch (the api.ts module fetch with token) and use it.
2026-08-10|167e22d|fix: hornet observer auto-evolve runs in a thread — the synchronous full-hive sweep (698 cells × Pisano freshness) froze the asyncio event loop on every boot/6h tick, hanging ALL HTTP requests (knowledge detail view showed '无法加载详情'); now asyncio.to_thread
2026-08-10|bdb2c24|fix: one-click research now carries the FULL knowledge body — new /v1/knowledge/{id}/resume-pack (full content + source + resonance related) used by list/emergent resume (was sending empty content, agent had to hunt for originals); emergent findings open a detail modal first (view → research/dismiss) instead of jumping straight into a session
2026-08-10|601015b|research-relay loop: parent_id chain (knowledge → research → new knowledge, linked via [知识来源ID] marker parsed at swarm ingestion) + resonance context pack (resume pulls top-3 related hive cells) + open local source in explorer (POST /v1/knowledge/reveal-source)
2026-08-10|5c3c305|knowledge click-to-view + continue-research: /v1/knowledge/{id}/detail (content + source link) + expandable knowledge rows (content preview, clickable URL or local path) + 🧠 resume button that opens a cowork session with the knowledge body as the first prompt (also from emergent findings in the hive)
2026-08-09|fe3abd5|E: knowledge-field health report — three-dimension assessment (structure/dynamics/evolution → 0-100 + rating) with weekly auto-emit in the observer loop + markdown report persisted to workspace + /v1/hornet/health & /v1/hornet/health-report + frontend health badge
2026-08-09|6acbfcb|review fixes for innovation batch A/B/C/D/F: C import remaps edges by REAL ids (was enum index — topology edges silently dropped on any non-contiguous-id hive) + A freshness downgrade reversible (no permanent z rewrite; batch transaction) + D rhythm_is_valley cached 60s (was blocking scheduler loop) + D anti-starvation (max 5 peak deferrals) + import payload validation
2026-08-09|8a5cd35|HORNET innovation batch A/B/C/D/F: period-driven freshness & forgetting (DPNN Pisano × load_factor → decay/downgrade to Z-) + knowledge field heatmap (tempColor gradient by amplitude/freshness) + cross-org hive alignment (export/import with phase-conflict → opposite edge + conflict emergent) + DPNN rhythm-aware scheduling (PeriodicForecaster gates heavy [HORNET] tasks during peaks) + self-questioning exploration (cavity → 3 hypothetical questions → search tasks)
2026-08-09|b64f636|adaptive hot-zone refinement off by default (experimental)
2026-08-09|80fea29|review fixes for the 5 HORNET×DPNN features: idempotent emergence actions (act only is_new; was duplicating tasks/knowledge every 6h) + phase-dominant interference (freq was a 1/25 hash-collision noise multiplier) + reversible failure feedback (failed_count instead of permanent z=-1 rewrite) + gap task fires immediately + fixed amplitude scale (was ÷300 on 0..1) + adaptive zone default off
2026-08-09|7ba5c40|packaging: build_env.ps1 — one-command packager that auto-probes libclang.dll (Python clang/LLVM/vcpkg), CMake and LLVM, sets env, then delegates to build_windows.ps1 (with -ProbeOnly for CI checks)
2026-08-09|178d5e4|HORNET low-compute spec: numpy topo-embed (2-layer GCN + ring-loss proxy, no torch/gudhi) as a build toggle (topo=true stores manifold embeddings, topo-aware layout + blended similar weights) + Hybrid-ODE-Sim (hot sub-blocks go continuous RK4, bulk stays discrete)
2026-08-09|42bea30|HORNET spec-conformance: cell fission (load_factor → Z+/Z- child cells) + cross-layer chain resonance (Z+↔Z- relay via XY) + topological cavities (never-resonated cells) + layered conflict (Z+ vs Z- opposition) + per-node resonance stats (node_hit_stats)
2026-08-09|bda6f73|HORNET self-organization loop: auto-evolve on boot + every 6h + after knowledge add/scan; emergence feed (/v1/hornet/emergence) deduped by (kind,title); global UI banner 'hive surfaced N new knowledge' with acknowledge — the human stays in the loop
2026-08-09|0bb608e|HORNET 3D: 12 channels G0-G11 (XY/Z+/Z- zones with asymmetric decay) + 12-dim phase + PCA-3D lattice layout + sim3d research module (Kelvin-cell wave sim + FFT) + isometric hive view with layer switching
2026-08-09|de03389|hornet: 2-gram Chinese matching + out-degree energy split + numpy PCA layout + title-gram prefilter
2026-08-09|66bcfda|fix: count_items docstring merged onto def line broke PyInstaller collect (coworker.knowledge.store IndentationError) — sidecar failed to boot
2026-08-09|d149dc2|HORNET 2D: 蜂巢共振神经拓扑知识层 — 六边形蜂房 + 六类语义边 + 图上共振检索 + 涌现观察者(/v1/hornet/*) + 前端蜂巢视图(Knowledge 页切换)
2026-08-08|1a2e20c|P0 fix: CSP connect-src missing http://127.0.0.1:* — Tauri injects the sidecar URL as 127.0.0.1, so the strict CSP (audit d3665ff) blocked every HTTP fetch (WS unaffected via ws: wildcard). Whole app appeared broken: settings stuck on Loading…, pages empty despite intact data.
2026-08-08|28e302c|install.ps1: fix PS5.1 UTF-8 BOM + CloseMainWindow method call
2026-08-08|f642924|anti-panic: connection banner + per-page load-error states (failures no longer read as 'data wiped'); graceful reinstall helper (packaging/install.ps1)
2026-08-08|bd868ed|deep-audit round 2: fix remaining 8 findings — #3 task_phase identity, #10 crash record_step, #11 timeout not accepted, #12 red-line execution intent, #14 capped re-revert, #16 dead code, #17 year-prefix parse, #18 step-gated governance
2026-08-08|91eb3bf|build: bump vite 5->6 + esbuild 0.25 (Node 24 compat); fix 6 TS errors from WsEvent discriminated union
2026-08-08|d3665ff|audit-fix: P0 security + P1 reliability + P2 type safety (owner-audit 2026-08-07)\n\nP0 (security/data sovereignty):\n- .gitignore: cover .coworker/ .qunwork/ .tmp-ws/ weekly_reports/ _swarm_reports/ .env *.log .DS_Store (prevent SQLite DB + token leak)\n- tauri.conf.json: CSP null -> strict policy (default-src 'self'; connect-src ipc ws localhost; img data blob; style unsafe-inline)\n- xlsx@0.18.5 (CVE-2023-30533) -> exceljs@4.4.0; SheetViewer rewritten\n\nP1 (reliability/correctness):\n- orchestrator._review: 2-attempt retry + degrade to low-conf accept (malformed reviewer JSON no longer crashes the whole asyncio.gather batch)\n- governance.drift/inspect: accept current_task param; orchestrator passes ready-batch head (drift no longer measures plan.tasks[-1])\n- RunController: cross-thread safe via loop.call_soon_threadsafe (set_paused/approve/reject from FastAPI thread)\n- PersistentVectorMemory: check_same_thread=False + threading.Lock\n- orchestrator._run: close self-created mem before return (SQLite leak across runs)\n- orchestration_tools: try/finally close OrchestrationRunStore\n- REVERT: reset tgt.retries=0 + recompute ready so reverted task re-dispatches THIS round\n- frontend: ErrorBoundary component + main.tsx mount; suggestion div a11y (role=button + keyboard); html lang syncs with i18n\n\nP2 (maintainability/observability):\n- logging introduced across orchestrator package; 11 bare except:pass -> logger.exception/debug\n- _persist_report OSError: emit report_save_failed event instead of silent swallow\n- vectorm6.2.py: cache query embedding once per search (was recomputing per item)\n- WsEvent: data:any -> discriminated union (18 branches); handleEvent per-case const d = ev.data narrowing\n- SwarmView: Record<string,any> -> Record<string,unknown>\n\ntests: 6 fixtures executor output padded to >=120 chars to avoid _looks_like_interim consuming reviewer turn (pre-existing bug surfaced by P1-4 retry)
2026-08-07|a229178|packaging: bundle rapidocr_onnxruntime (+onnxruntime) for vision skill OCR in sidecar
2026-08-07|ce87c49|vision: image-understanding skill (RapidOCR + optional VL API) for text-only models + image attachments persisted to workspace ([image: path] in context)
2026-08-07|dbe8536|organization page: home nav entry + asset overview tiles + reusable org cards (trust surface)
2026-08-07|2f3c1f4|shell: prefer pwsh (PS7) + chcp 65001 + InputEncoding + spawn-sync ready marker + non-ASCII commands auto-wrapped in UTF-8-BOM .ps1 — chinese args/paths survive cold-start code-page race (5.1 falls back cleanly)
2026-08-07|f2659da|i18n: inbox (Pending/Configure tabs, routing/unrouted/approval copy) + connectors detail labels (Listening/Waiting/Default/Accounts/Recommended/Access/Tools/Portals/Sandbox/private app/no matches/RECENT etc.) localized
2026-08-06|f87b3fb|post health-check fixes: UTF-8 for all remaining subprocess text runs (git/zenity/rg — GBK reader death), _finalize keeps completed status on post-success errors + logs orchestration_error event
2026-08-06|a806cf9|test task: '组织资产健康体检' benchmark template (idempotent seed — new templates appear even when others exist)
2026-08-06|48b3fb2|frontend asset panels: Organizational rhythm card (cadence detection + next-week due), asset lifecycle UI (retire/restore knowledge, use_count badge, content expand) in OrgAssetsCard
2026-08-06|d1ff87a|asset loop Phase 3: asset lifecycle (knowledge use_count/retired + retire API), calendar phase slots for scheduled tasks (weekly=ISO-week, daily=year-day), organizational rhythm forecast /v1/rhythm/forecast (T6 on org cadence + next-week due list), weak-template governance WARN, cross-org package preserves provenance
2026-08-06|c5121cb|asset loop Phase 2: unified asset search /v1/assets (knowledge+skills+templates+memories+runs), skills keyword-searchable, knowledge_items.source_run_id cross-index (automation/swarm runs sink with their run id)
2026-08-06|f6a57da|asset loop Phase 1 (interconnect): automation results + swarm reports auto-sink into unified knowledge (kind=automation/swarm_report); template track record auto-tallied via template_id; worker engines get team-memory tools + unified knowledge DB (no path split)
2026-08-06|4d14758|worker tool heartbeat: TOOL_STARTED/FINISHED tick the run stream + deck stale window relaxed to max(10m, 3x budget) — consolidation task no longer misjudged as dead
2026-08-06|7f1a517|weekly-report delivery: executor max_iterations 24 + interim-note deliverable push + requeue timeout 30s + task timeout 240s; SwarmView run button accent-blue with pulse
2026-08-06|8c125db|fix: executor crash in git repos with Chinese commits — environment._git subprocess text decode forced UTF-8 + None-guard (GBK reader death returned stdout=None; 0/6 stalled)
2026-08-06|369e282|fix: Skip on the requeue deck accepts the current result (degraded, conf<=0.4) so dependents proceed — skipped task no longer deadlocks the swarm at needs_human
2026-08-05|79109c2|fix weekly-report timeout: UTF-8 root-fix in shell executor (PYTHONUTF8 + console encoding + utf-8 pipe decode) + soft-budget deadline so consolidation task survives a near-miss timeout
2026-08-05|b8fa837|5.2.2 team memory panel: search/edit/forget API (GET/POST search/PUT/DELETE /v1/memory) + TeamMemoryCard UI
2026-08-05|b38a368|strategy report follow-ups: template track record (5.2.1) + governance tally card (5.2.3) + auto coordination report (5.2.4)
2026-08-05|c7e946b|T5 in scheduler: periodic phase slot + prior-result reuse for scheduled runs (weekly report builds on last week)
2026-08-05|7f8aca2|P2: T6 periodic forecaster + team workspace (export/import package) + DPNN reproduction baseline (periodic prior beats AR 12x on synthetic)
2026-08-05|72fb6ff|benchmark showcase: coordination report renderer (/v1/orchestrate/{run_id}/report) + 3 seeded swarm templates + SwarmView 'Coordination report' button
2026-08-05|a0b7496|P1 T4+T5: convergence guard (stall detection, run_stalled) + phase-indexed memory (Pisano-period slots, phase-preferring search)
2026-08-05|8bd30e6|G2: SwarmView command deck — pause/resume, operator messages, requeue approve/reject (RunController + /v1/orchestrate/{id}/control)
2026-08-05|7c98b19|fix: broken line join in app.py (decorator+def on one line) that crashed the frozen sidecar
2026-08-05|789e514|dev-plan round: G1 swarm open to cowork (+SWARM guidance) · T1 FPA periodic tools (pisano table + recurrence verify) · G3 swarm 'save as template' + /v1/swarm-templates
2026-08-05|2e4117f|fix: artifacts include the session workspace's PARENT (primary workspace) in both listing and read resolution
2026-08-05|c639412|fix: artifacts panel lists session+primary workspace deliverables, skips dep/build trees (sidecar noise was crowding out real files)
2026-08-05|f757d2f|fix: artifacts open cross-workspace + URL-encoded (Chinese) filenames — read_artifact accepts absolute paths, primary-workspace fallback, %-decoding
2026-08-05|2772008|ui: show complete brand lockup (Q+swarm + 群沃客) on session intro
2026-08-05|a559f08|fix: stray brace in styles.css breaking vite build
2026-08-05|a1d5fe7|ui: show real QunWork brand mark (Q+swarm PNG) in sidebar brand row + boot splash
2026-08-05|765b73a|brand: slogan → '群沃客——让协作自然发生' (QunWork — collaboration, naturally.)
2026-08-05|aebf876|feat: task mental model round 2 — session default 'New task', user task templates (CRUD API + home cards), brand slogan '群策群力,沃客未来' in Sidebar+Onboarding
2026-08-05|d74cfc4|feat: home session area → task/project mental model (new-task entry, task-first copy, composer placeholder)
2026-08-05|1c5634f|i18n: connectors detail pages + MCP remaining blocks localized (77 new keys, ~420 total)
2026-08-04|3603d42|i18n: manual language switch (Settings → 语言, persisted in localStorage qunwork-lang) + tests
2026-08-04|40dd821|fix: escape inner quotes in i18n dict entry
2026-08-04|0e0d8ff|i18n: deep zh localization — SettingsView/ProviderSetup/ApprovalCard/connectors list/MCP tabs +插值默认修复
2026-08-04|4d9102a|fix: upstream andrewyng/openworker community issues (verified in our tree)