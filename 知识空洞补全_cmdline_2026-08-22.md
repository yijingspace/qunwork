# 知识空洞补全：cmdline.rst — Python 标准库模块命令行接口

> **空洞节点**: `cmdline.rst` (ID 58040)
> **空洞原因**: 该节点无语义连接，是 Python 官方文档中"Modules command-line interface (CLI)"目录页，列出所有拥有 CLI 的标准库模块
> **补全日期**: 2026-08-22

---

## 1. 节点概要

`cmdline.rst` 是 Python 标准库文档的**模块 CLI 索引页**，它不包含具体实现，而是指向以下模块各自的 CLI 文档：

| 模块 | CLI 功能 | 典型用法 |
|------|----------|----------|
| `ast` | 语法树编译/转换 | `python -m ast -m exec -f ast - < file.py` |
| `asyncio` | 异步事件循环策略工具 | `python -m asyncio --help` |
| `base64` | Base64/Base32/Base85 编解码 | `echo -n hello \| python -m base64` |
| `calendar` | 日历显示与转换 | `python -m calendar 2026` |
| `code` | 交互式解释器 | `python -m code` |
| `compileall` | 批量编译 .py → .pyc | `python -m compileall src/` |
| `cProfile` / `profile` | 性能分析器 | `python -m cProfile script.py` |
| `dis` | 字节码反汇编 | `python -m dis script.py` |
| `ensurepip` | pip 引导安装 | `python -m ensurepip --upgrade` |
| `hashlib` | 哈希计算工具 | `python -m hashlib sha256 < file` |
| `http.server` | 快速 HTTP 服务器 | `python -m http.server 8080` |
| `json.tool` | JSON 格式化/校验 | `cat data.json \| python -m json.tool` |
| `pdb` | 交互式调试器 | `python -m pdb script.py` |
| `py_compile` | 单文件编译 | `python -m py_compile script.py` |
| `pydoc` | 文档生成与查看 | `python -m pydoc os` |
| `site` | 站点包路径检查 | `python -m site --user-site` |
| `sqlite3` | SQLite 交互式 shell | `python -m sqlite3 db.sqlite` |
| `tabnanny` | 缩进检查 | `python -m tabnanny src/` |
| `tarfile` | tar 归档操作 | `python -m tarfile -l archive.tar.gz` |
| `timeit` | 代码片段计时 | `python -m timeit "sum(range(1000))"` |
| `tokenize` | 源码词法分析 | `python -m tokenize < script.py` |
| `tomllib` | TOML 解析与验证 | `python -m tomllib pyproject.toml` |
| `unittest` | 单元测试运行器 | `python -m unittest discover tests/` |
| `venv` | 虚拟环境创建 | `python -m venv .venv` |
| `zipapp` | Python zip 应用打包 | `python -m zipapp myapp/` |
| `zipfile` | zip 归档操作 | `python -m zipfile -l archive.zip` |

---

## 2. 与 Python CLI 体系的关联

### 2.1 两个层面的命令行接口

Python 的命令行存在**两个不同文档页面**，容易混淆：

| 页面 | 文档位置 | 内容 |
|------|----------|------|
| **Python 解释器 CLI** | `using/cmdline.html` | `python` 命令本身的参数（`-c`, `-m`, `-O`, `-B`, `-X` 等） |
| **标准库模块 CLI** | `library/cmdline.html` | 各模块 `python -m <module>` 的参数 |

两者的交叉点是 `-m` 标志：它是解释器层面的参数，用来调用模块层面的 CLI。

### 2.2 Python `-m` 执行机制

```
python -m <module> [args]
```

等价于 `python -m runpy.run_module`，执行链：
1. 解析 `-m` 标志 → 查找模块（sys.path 搜索）
2. 如果模块有 `__main__.py`，执行它
3. 如果模块文件本身可运行，直接执行
4. 模块自行解析剩余 `sys.argv[1:]`

### 2.3 构建自己的 CLI：标准工具链

| 工具 | 适用场景 | 复杂度 |
|------|----------|--------|
| `argparse` | 标准库 CLI 构建器，子命令支持 | 中 |
| `click` | 装饰器式 CLI 框架，类型自动推断 | 低 |
| `typer` | 基于 type hints 的 CLI（自动文档） | 最低 |
| `setuptools` entry_points | `pip install` 后注册为系统命令 | — |

---

## 3. 关键模块详解（本工作区高频使用）

### 3.1 `http.server` — 本地 HTTP 服务器

**在本工作区中的核心用途**：Playwright 浏览器自动化要求 `http://` 或 `https://` URL（`file://` 不支持），因此必须先起本地服务器。

```powershell
# 起一个临时 HTTP 服务器（Python 3.14）
cd E:\QunWork\目标目录
python -m http.server 8765
# 浏览器访问 http://localhost:8765/page.html
```

**关键参数**：
- `--directory <path>` — 指定根目录（默认当前目录）
- `--bind <addr>` — 绑定地址（默认 `0.0.0.0`）
- `--port <n>` — 端口号（默认 8000）

**注意事项**：
- 单线程，不适合生产环境
- 用完需手动关闭端口
- Windows 防火墙可能弹窗

### 3.2 `json.tool` — JSON 格式化

```powershell
cat data.json | python -m json.tool        # 格式化输出
cat data.json | python -m json.tool --no-ensure-ascii  # 保留非 ASCII 字符
```

### 3.3 `timeit` — 性能计时

```powershell
python -m timeit -n 1000 -r 5 "sum(range(1000))"
```

在 Python 代码中调用：
```python
import timeit
timeit.timeit('sum(range(1000))', number=1000)
```

### 3.4 `pdb` — 调试器

```powershell
python -m pdb script.py
# 交互命令: n(步进), s(进入函数), c(继续), b(断点), p(打印变量), q(退出)
```

### 3.5 `unittest` — 测试运行

```powershell
python -m unittest discover -s tests -p "test_*.py" -v
```

---

## 4. 与 QunWork 工具链的连接

### 4.1 本工作区常用模块 CLI 速查

| 场景 | 命令 | 本工作区实例 |
|------|------|-------------|
| 起 HTTP 服务（Playwright 前置） | `python -m http.server <port>` | 渲染截图、验证 HTML |
| JSON 处理 | `python -m json.tool` | API 响应调试 |
| 性能对比 | `python -m timeit` | 脚本优化 |
| 测试运行 | `python -m unittest` | 验证脚本正确性 |
| 虚拟环境隔离 | `python -m venv` | 新项目环境创建 |
| 依赖安装 | `python -m pip install` | 工具链安装 |

### 4.2 subprocess 调用 vs 模块 CLI

在本工作区的自动化脚本中，常用 `subprocess.run()` 调用这些模块 CLI：

```python
# 推荐写法：直接模块调用（避免 python 路径问题）
import subprocess, sys

# 格式化 JSON
subprocess.run([sys.executable, "-m", "json.tool", "data.json"], capture_output=True, text=True)

# 起 HTTP 服务器（后台）
server = subprocess.Popen(
    [sys.executable, "-m", "http.server", "8765"],
    cwd="target/dir"
)
# ... 完成后
server.terminate()
```

**关键原则**：
- 用 `sys.executable` 而非硬编码 `python` 路径
- 用 `-m` 调用模块 CLI，确保在正确 Python 环境中运行
- Windows 上 `subprocess.run` 默认通过 `cmd.exe`，需要 `shell=True` 或列表参数形式

---

## 5. Python 解释器 CLI 关键标志速查

（来自 `using/cmdline.html`，与模块 CLI 配合使用）

| 标志 | 功能 | 示例 |
|------|------|------|
| `-m <mod>` | 运行模块作为脚本 | `python -m http.server` |
| `-c <cmd>` | 执行字符串代码 | `python -c "print(42)"` |
| `-O` | 移除 assert 和 `__debug__` | 性能优化 |
| `-B` | 不生成 .pyc 文件 | 容器/Docker 中使用 |
| `-X <opt>` | 启用特定实现选项 | `-X utf8`, `-X dev` |
| `-u` | 无缓冲 stdout/stderr | 实时日志输出 |
| `-v` | 详细导入日志 | 调试模块加载问题 |
| `-W <action>` | 警告控制 | `-W error::DeprecationWarning` |
| `--help` | 显示完整帮助 | 探索可用选项 |

---

## 6. 边缘场景与陷阱

### 6.1 Windows 下的中文路径问题

Python 标准库模块 CLI 在 Windows 上处理中文路径时可能出现编码问题：

```powershell
# ❌ 中文文件名可能失败
python -m compileall "中文目录/"

# ✅ 方案1：切换到目标目录
cd "中文目录" && python -m compileall .

# ✅ 方案2：设置环境变量
$env:PYTHONIOENCODING = "utf-8"
python -m compileall "中文目录/"
```

### 6.2 端口冲突

`http.server` 起服务时端口可能被占用：

```powershell
# 检查端口占用
netstat -ano | findstr :8765
# 杀掉占用进程
taskkill /PID <pid> /F
```

### 6.3 `-m` 与直接文件执行的区别

```
python -m json.tool data.json    # ✅ 通过 runpy 机制执行
python json/tool.py data.json    # ❌ 找不到文件（模块不是 .py）
```

`-m` 搜索已安装的包，而直接文件执行需要完整路径。

---

## 7. 知识图谱连接

本条目补全了 `cmdline.rst` 孤立节点，建立以下语义连接：

```
cmdline.rst (节点 58040)
├── → Python 解释器 CLI (using/cmdline.html)  [实现层面]
├── → argparse 模块  [CLI 构建器]
├── → subprocess 模块  [系统调用/管道]
├── → sys.argv / sys.exit  [程序入口/出口]
├── → http.server  [本工作区高频使用]
├── → json.tool  [数据处理]
├── → pdb / timeit / unittest  [开发工具链]
├── → setuptool entry_points  [CLI 打包分发]
└── → QunWork 自动化脚本  [实践应用]
```

---

## 8. 总结

`cmdline.rst` 作为 Python 标准库模块 CLI 的索引页，是连接**Python 解释器层面**（`using/cmdline.html`）与**具体模块实现**的枢纽。在本工作区中，最高频使用的模块 CLI 是 `http.server`（Playwright 截图前置服务）和 `json.tool`（数据处理调试）。理解 `-m` 执行机制、`sys.executable` 的跨环境正确性、以及 Windows 编码陷阱，是有效利用这些工具的关键。
