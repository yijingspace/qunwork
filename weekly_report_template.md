# 周报模板（可复用 · 同相位结构）

> 模板定位：**每周直接运行**的周报骨架。三个固定章节（本周成果 / 达成度 / 下周计划）构成"同相位"结构——
> 每周运行的是**同一个相位**的模板，通过自动复用**上周历史报告**形成闭环：
> 上周「三、下周计划」→ 本周「二、达成度评估」的目标基线；本周「三、下周计划」→ 下周基线。
> 章节标题是**稳定约定**，`scripts/weekly_report_run.py` 据此自动抽取上周基线，请勿改动标题文字。

---

## 📋 一、本周工作事项与成果

> 来源：t0/t2 章节｜git 提交、产出物、_swarm_reports/、.state 运行记录
> 自动填入：提交数、逐日分布、diff 行数、产出物目录；定性内容按下列小节填写。

### 1.1 本周关键成果（3–5 条）
- **[成果标题]**：一句话描述 + 支撑证据（commit 号 / 文件路径 / 链接）

### 1.2 本周关键数据（自动生成，含上周环比）
| 指标 | 本周 | 上周 | 环比 |
| --- | --- | --- | --- |
| 提交数 | `{this_commits}` | `{last_commits}` | `{delta_commits}` |
| 新增/删除行 | +`{this_ins}` / −`{this_dels}` | +`{last_ins}` / −`{last_dels}` | — |
| 里程碑 | `{milestone}` | — | — |

### 1.3 本周产出物清单
- [产出物]（路径 `…`，价值一句话）

---

## 🎯 二、目标达成度评估

> 来源：t1 章节｜**基线 = 上周报告「三、下周计划」（自动复用，见下方"复用基线"）**
> 逐项给出 达成 / 部分达成 / 未达成 与差距；状态枚举值固定，便于下周自动统计。

### 2.1 上周目标基线（自动复用自 `{prev_report}`）
| 上周目标 | 优先级 | 本周状态 | 证据 | 差距说明 |
| --- | --- | --- | --- | --- |
{goal_rows}

### 2.2 总体达成度
- 达成 **X / Y** 项，达成率 **≈ Z%**；总体结论：…

### 2.3 未达成项原因与改进
- [原因] → [下周改进动作]

---

## 🗓 三、下周计划

> 来源：t4 章节｜**本节的"目标"行将成为下周报告 2.1 的自动基线**，请写清可验收的目标。

### 3.1 下周目标（供下周自动复用）
- [目标]（一句话，含可验收结果）

### 3.2 具体任务（优先级 + 验收标准）
- [P0] 任务描述 —— 验收：…
- [P1] 任务描述 —— 验收：…
- [P2] 任务描述 —— 验收：…

### 3.3 风险与依赖
- [风险 / 所需支持]

---

## 🔁 复用逻辑（每周自动执行）

### 数据链路
```
today ──▶ 本周区间(周一~周日) ──▶ 上周区间 = 本周周一 − 7d
                                      │
       ┌──────────────────────────────┤
       ▼                              ▼
   git 本周数据               查找上周报告（weekly_reports/ + _swarm_reports/，
  (提交/逐日/diff)            取上周区间内 mtime 最新 .md）
                                      ▼
                              抽取「三、下周计划」→ 本周 2.1 基线
                                      ▼
                             渲染模板 → weekly_reports/{YYYY-Www}_周报.md
```
- 同相位：每周报告 = 同一模板同一相位；复用的是**上一周期（上周）**的结果（Pisano 相位槽 `run_no % 60`，模 10 周期 = 60，已由 `coworker/server/manager.py` 调度器注入 prior result）。
- 自动注入：调度器运行时会自动携带 `[prior result to build on]`（上次运行结果前 800 字符），脚本再独立完成上周报告全文检索与「下周计划」抽取，双保险。
- 找不到上周报告：基线留空并标注，不阻断生成。

### 运行方式
1. **手动立即生成本周报告**
   ```bash
   python scripts/weekly_report_run.py            # 用今天日期
   python scripts/weekly_report_run.py --date 2026-08-06   # 指定日期
   ```
2. **定时每周自动运行**（复用内置调度器，`create_scheduled_task`）
   - cron：`0 18 * * 5`（每周五 18:00）
   - instructions：使用本模板结构与 `scripts/weekly_report_run.py` 取数，将 prior result（上周报告）的「三、下周计划」作为本周达成度基线，按 一/二/三 三节输出完整周报，写入 `weekly_reports/2026-Wxx_周报.md`，关键数字与 git 统计交叉核验。

### 校验规则（写报告前自查）
- [ ] 三章齐全：一(成果) / 二(达成度) / 三(下周计划)，标题逐字一致
- [ ] 提交数、diff 行数来自脚本输出，未手改
- [ ] 达成度状态只用 ✅/🟡/❌ 三种枚举
- [ ] 2.1 基线行数 ≥ 上周 3.1 目标行数（无遗漏）
